# 系统级场景预设 —— 一整套仿真条件打包成一个名字。
#
# 结构：channel_preset（复用信道预设，不重新定义）+ generate（生成覆盖）
#      + system（sr_system_sim 参数）+ expect（实测锚点）
#
# 两条硬规矩：
# 1. expect 只写**实测过**的值，measured: false 时不许拿设计意图充数。
#    CLAUDE.md：「preset 里的 label 是设计意图，写着「高干扰」实际只有 2 dB 的事发生过」
# 2. pair_with 声明的对照组，除了 pair_varies 列出的那几项，其余必须逐字相同。
#    sysscenes.check_pairs() 会强制校验——同时差三四个参数的两个场景不是对照组。
#
# 所有场景都要求每 UE 的有效快照数
# num_slots_per_sample * num_samples / num_ues >= 8。当前推荐 num_slots_per_sample=1
# 且每 UE 至少 8 个样本，以绕开外部 ChannelHub 多时隙 SIR/SINR 聚合口径尚未统一的问题。
# 2026-08-07 的数值锚点全部来自 physics audit 前的 legacy_v1，仅用于历史回归，
# 不得当成 2026-08-09 修正后实现的当前验收值；expect.model_version 会显式标记。

# ---------------------------------------------------------------------------
# 一、负载对照组：满载 vs 轻载
# ---------------------------------------------------------------------------
sys_dense_urban_loaded:
  group: 系统级 · 负载对照
  label: 密集城区满负载 · 21 小区 ISD 150 m · 邻区 90%
  summary: >-
    现网密集城区的高干扰工况，ISD 150 m、邻区 PRB 利用率 0.9 接近满负载。
    **别拿它看 IoT**：这个场景要求多时隙（PF 分集与 CSI 老化的前提），
    而多时隙下 IoT 的两个输入量口径不一致，算出来是坏的——
    详见 expect.note。要看 IoT 请单独生成一批 num_slots_per_sample=1 的。
  answers:
    - 密集城区满负载下用户实际体验到多快
    - 边缘用户被压到什么程度
    - 满载与轻载的体验差多少（与 sys_dense_urban_light 配对）
  channel_preset: high_iot_dense
  pair_with: sys_dense_urban_light
  pair_varies: [neighbor_prb_util, arrival_rate_hz]
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 3.0
  system:
    duration_s: 3.0
    traffic_model: ftp3
    arrival_rate_hz: 3.0
    neighbor_prb_util: 0.9
    csi_aging: true
    srs_period_ms: 10.0
  expect:
    measured: false
    invalidated_by: >-
      2026-09-03 合并容量/体验模式：legacy_v1 全带路径删除，所有场景改走
      experience_v2（容量口径 = traffic_model: full_buffer）。下面的数字是
      legacy_v1 测的，口径已变，**不再成立**。重跑 sr_probe_scenario 后再填。
    model_version: experience_v2
    dataset: ds_9625340c
    n_replications: 8
    # 格式一律是 [均值, 95% 区间半宽占均值的百分比]。**半宽和数值不可分割**——
    # 单独引用均值就是在假装这次实验的分辨率无限高。
    note: >-
      2026-08-07 实测，8 次重复。**IoT 故意不记**：这个数据集
      num_slots_per_sample=8，而多时隙下 sinr_dB 是各 slot 的 dB 均值、
      sir_dB 只取最后一个 slot，两者不同口径——实测 sir−sinr 中位是 -0.53 dB
      （物理上必须为正），80 个样本 43 个算不出 IoT。
      信道预设 high_iot_dense 的 expect 写着 iot_dl_db 35.76（单时隙探测测的），
      系统级同配置算出来 2.19 dB，**差 33 dB，那个 2.19 是坏的不是低的**。
      **多时隙又是 PF 分集与 CSI 老化的前提，所以这两个需求当前无法在
      同一个数据集上同时满足**，要看 IoT 得单独生成一批单时隙的。
      体验速率两项半宽超过 20%，只能读量级，要收窄得加长 duration_s 或加重复次数。

sys_dense_urban_light:
  group: 系统级 · 负载对照
  label: 密集城区轻载 · 同拓扑 · 邻区 10%
  summary: >-
    与 sys_dense_urban_loaded **同一批信道、同一撒点**，只把邻区负载降到 0.1、
    话务到达率降低。两者之差就是负载本身的代价，不混其他因素。
  answers:
    - 负载从 10% 涨到 90%，体验速率掉多少
    - 轻载时是不是噪声受限
  channel_preset: high_iot_dense
  pair_with: sys_dense_urban_loaded
  pair_varies: [neighbor_prb_util, arrival_rate_hz]
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 3.0
  system:
    duration_s: 3.0
    traffic_model: ftp3
    arrival_rate_hz: 1.0
    neighbor_prb_util: 0.1
    csi_aging: true
    srs_period_ms: 10.0
  expect:
    measured: false
    invalidated_by: >-
      2026-09-03 合并容量/体验模式：legacy_v1 全带路径删除，所有场景改走
      experience_v2（容量口径 = traffic_model: full_buffer）。下面的数字是
      legacy_v1 测的，口径已变，**不再成立**。重跑 sr_probe_scenario 后再填。
    model_version: experience_v2
    dataset: ds_9625340c
    n_replications: 8
    note: >-
      2026-08-07 实测，8 次重复。与 sys_dense_urban_loaded 同一批信道。
      **注意方向**：轻载的体验速率(106) 高于满载(33)，而承载吞吐(33) 低于满载(87)
      ——体验速率衡量的是"轮到我时有多快"，满载时人人都在排队。
      **拿这两个场景对比时必须用公共随机数**（两臂同一批 replication 流），
      否则 5% 边缘那一项 38% 的半宽会把差异整个淹掉。
      IoT 同样不记，原因见对照组。

# ---------------------------------------------------------------------------
# 二、CSI 时延对照组：现网 SRS 配置 vs 零时延上界
# ---------------------------------------------------------------------------
sys_urban_macro_field_csi:
  group: 系统级 · CSI 时延对照
  label: 城区宏站 · 现网 SRS 配置（10 ms + 17 倍跳频）
  summary: >-
    38.211 Table 6.4.1.4.3-1 的 C_SRS=63：每跳 16 RB 正好一个 RBG，
    17 跳扫完 272 RB。10 ms 周期下全带扫一遍要 170 ms，
    平均 CSI 陈旧时长 87 ms——**这是默认配置，也是现网的样子**。
  answers:
    - 现网 SRS 配置下体验速率是多少
    - CSI 老化吃掉了多少
  channel_preset: company_64t4r_multicell
  pair_with: sys_urban_macro_ideal_csi
  pair_varies: [csi_aging]
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 30.0
  system:
    duration_s: 3.0
    traffic_model: ftp3
    arrival_rate_hz: 2.0
    neighbor_prb_util: 0.3
    csi_aging: true
    srs_period_ms: 10.0
  expect:
    measured: false
    invalidated_by: >-
      2026-09-03 合并容量/体验模式：legacy_v1 全带路径删除，所有场景改走
      experience_v2（容量口径 = traffic_model: full_buffer）。下面的数字是
      legacy_v1 测的，口径已变，**不再成立**。重跑 sr_probe_scenario 后再填。
    model_version: experience_v2
    dataset: ds_984082de
    n_replications: 8
    note: >-
      2026-08-07 实测，8 次重复。**替换了 2026-08-04 的单次运行值，
      而且替换本身就是这项工作的价值证明**：旧的单次值 5% 边缘是 123.5，
      新的 8 次均值是 101.3 —— 偏了 22%，是半宽的两倍，那次单跑抽到了离群样本。
      与零时延对照组的差是 -52.7%，远大于两边各自 6~9% 的半宽，
      **所以「CSI 老化吃掉一半体验速率」这个结论依然站得住**。

sys_urban_macro_ideal_csi:
  group: 系统级 · CSI 时延对照
  label: 城区宏站 · 零时延完美 CSI（上界，不是现网）
  summary: >-
    与 sys_urban_macro_field_csi **除 csi_aging 外逐字相同**。
    预编码与评估用同一个信道矩阵——**这是上界不是现网**，
    它会系统性高估 MU 增益，只用来量"老化吃掉了多少"。
  answers:
    - CSI 老化的代价有多大（拿它当分母）
  channel_preset: company_64t4r_multicell
  pair_with: sys_urban_macro_field_csi
  pair_varies: [csi_aging]
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 30.0
  system:
    duration_s: 3.0
    traffic_model: ftp3
    arrival_rate_hz: 2.0
    neighbor_prb_util: 0.3
    csi_aging: false
    srs_period_ms: 10.0
  expect:
    measured: false
    invalidated_by: >-
      2026-09-03 合并容量/体验模式：legacy_v1 全带路径删除，所有场景改走
      experience_v2（容量口径 = traffic_model: full_buffer）。下面的数字是
      legacy_v1 测的，口径已变，**不再成立**。重跑 sr_probe_scenario 后再填。
    model_version: experience_v2
    dataset: ds_984082de
    n_replications: 8
    note: >-
      2026-08-07 实测，8 次重复。**olla_db_mean 那 58.9% 不是这一项不可信**——
      均值只有 -0.27，相对半宽的分母太小，绝对半宽其实只有 0.16 dB。
      **接近零的量一律看绝对区间，别看相对半宽。**
      零时延下 OLLA 本就该贴近 0（发送侧估计准，外环没什么要修的），
      这和现网配置那边被压到 -4.21 dB 正好互为印证。

# ---------------------------------------------------------------------------
# 三、现网话务画像
# ---------------------------------------------------------------------------
sys_field_bimodal_traffic:
  group: 系统级 · 话务
  label: 现网话务画像 · 两头高中间低（30% 小包 / 30% 满包）
  summary: >-
    用户给的现网口径：小包约占 30%、满包约占 30%、中间相对均匀。
    **口径已迁移**：退役的 bimodal 是"按占用 RBG 数"抽样再乘一个写死的
    3.0 bit/s/Hz 折成字节；现在改成 cdf 驱动的**包长分布**，
    一次传输实际占几个 RBG 由该用户当时的 MCS/rank 经 TBS 反查决定。
    两份 CDF 由 scripts/make_field_bimodal_cdf.py 机械转写自原旋钮，
    **不是新的现网测量**。小包不再是盲区：Rel-19 的 fractional-slot 口径可测。
  answers:
    - 现网话务下 PRB 利用率是多少
    - 大包用户和小包用户的体验差多少
  channel_preset: company_64t4r_multicell
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 3.0
  system:
    duration_s: 5.0
    traffic_model: cdf
    packet_size_cdf: presets/traffic_cdf/field_bimodal_packet_size.csv
    interarrival_cdf: presets/traffic_cdf/field_bimodal_interarrival.csv
    neighbor_prb_util: 0.3
    csi_aging: true
  expect:
    measured: false
    invalidated_by: >-
      2026-09-03 合并容量/体验模式：legacy_v1 全带路径删除，所有场景改走
      experience_v2（容量口径 = traffic_model: full_buffer）。下面的数字是
      legacy_v1 测的，口径已变，**不再成立**。重跑 sr_probe_scenario 后再填。
    model_version: experience_v2
    dataset: ds_6e9715bc
    n_replications: 8
    note: >-
      2026-08-07 实测，8 次重复。**首传 BLER 0.883 不是笔误，是这个场景的真实
      失效模式**：小包 burst 太小，占用率只有 10.7%，5 秒里 OLLA 拿不到足够的
      ACK/NACK 就永远收敛不了（偏置才压到 -2.05 dB，稳态需要更负）。
      **这个场景当前不能用来下 BLER 或体验速率的结论**——
      体验速率两项半宽 49% 与 84%，本身也分辨不出来。
      要让它可用：加长 duration_s 到 20 秒以上，或临时开 olla_speedup。
      另：折合 PRB 利用率 37.1% vs 现网口径 30%，差在 2~16 均匀分布的均值偏高，
      **这个参数还没对齐**（调 p_idle_tti 没用，它不驱动仿真）。

# ---------------------------------------------------------------------------
# 四、移动性
# ---------------------------------------------------------------------------
sys_mobility_60kmh:
  group: 系统级 · 移动性
  label: 中速移动 · 60 km/h · 现网 SRS 配置
  summary: >-
    2.6 GHz 下 60 km/h 的多普勒是 144 Hz，Jakes 相干时间约 1.7 ms，
    而平均 CSI 年龄 87 ms——**是相干时间的 50 倍**，预编码基本在对
    一个无关的信道做匹配。实测 SVD 权体验速率掉 57%（320.0 → 138.3）。
  answers:
    - 中速移动下 CSI 老化有多致命
    - SVD 权和 Type I 码本权哪个更耐受老化
  channel_preset: company_64t4r_multicell
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 60.0
  system:
    duration_s: 3.0
    traffic_model: ftp3
    arrival_rate_hz: 2.0
    neighbor_prb_util: 0.3
    csi_aging: true
    srs_period_ms: 10.0
    olla_speedup: 20.0
  expect:
    measured: false
    invalidated_by: >-
      2026-09-03 合并容量/体验模式：legacy_v1 全带路径删除，所有场景改走
      experience_v2（容量口径 = traffic_model: full_buffer）。下面的数字是
      legacy_v1 测的，口径已变，**不再成立**。重跑 sr_probe_scenario 后再填。
    model_version: experience_v2
    dataset: ds_0ef213d2
    note: >-
      2026-08-04 实测（olla_speedup=20 以便 3 秒内收敛）。
      同配置零时延是 320.0 Mbps。

# ---------------------------------------------------------------------------
# 五、容量上界（不看体验速率）
# ---------------------------------------------------------------------------
sys_capacity_full_buffer:
  group: 系统级 · 容量上界
  label: 容量仿真 · 话务开到最大（full buffer）
  summary: >-
    **"容量仿真"就是这个场景**：它不是另一条仿真分支，只是把话务开到最大。
    缓冲区永不空 ⇒ 按需 RBG 反查恒等于全带宽、每 TTI 一个 SU（或一对 MU），
    调度、AMC、HARQ、解调 SINR 聚合全部照体验口径走。
    两个体验速率口径都有值：ITU 口径 ue_served_p5_mbps 与 28.552 口径
    drb_throughput_rel19_mbps（在飞 busy period 的窗内段照常统计），
    满缓冲下两者收敛。只有需要 burst 真的传完的键报 None（完成时延、PDB、
    含头速率）。结果里引用 cell_served_mbps、ue_served_p5_mbps、
    drb_throughput_rel19_mbps、PRB 利用率与 Jain 公平度。
  answers:
    - 这个小区的吞吐上界是多少
    - PF 在满负载下的公平性如何
  channel_preset: company_64t4r_multicell
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 3.0
  system:
    duration_s: 3.0
    traffic_model: full_buffer
    neighbor_prb_util: 0.3
    csi_aging: true
  expect:
    measured: false
    invalidated_by: >-
      2026-09-03 合并容量/体验模式：legacy_v1 全带路径删除，所有场景改走
      experience_v2（容量口径 = traffic_model: full_buffer）。下面的数字是
      legacy_v1 测的，口径已变，**不再成立**。重跑 sr_probe_scenario 后再填。
    model_version: experience_v2
    dataset: ds_6e9715bc
    n_replications: 8
    note: >-
      2026-08-07 实测，8 次重复（数值已因口径变更作废，见 invalidated_by）。
      **体验速率是 None**：full buffer 下缓冲区永不清空，28.552 的 DRB
      合并前的 legacy 路径报的是 0，
      那是在假装"测到了 0 Mbps"；现在报 None 并在 notes 里说明。
      **别把它当成 bug 去修。**
      注意所有区间都极窄（≤0.2%）——因为 full buffer 没有话务随机性，
      唯一的随机源只剩 HARQ 抽样。**这反过来说明其他场景那些宽区间主要来自话务**。

# ---------------------------------------------------------------------------
# 六、experience_v2：按需 RBG 的 PF 记账反向控制
# ---------------------------------------------------------------------------
sys_experience_mixed_pf:
  group: 系统级 · 体验模式 PF 记账
  label: 大小文件混跑 · scheduled-TBS PF（正确口径）
  summary: >-
    experience_v2 里同一 TTI 可给多个 UE 分 RBG，PF 平均速率必须按实际 scheduled TBS
    更新。与反向控制组只差 pf_accounting，用于验证全带记账是否会饿死小文件 UE。
  answers:
    - 按需 RBG 后小文件用户的排队和完成时延是否改善
    - PF 平均速率按全带记账会造成什么反向效果
  channel_preset: company_64t4r_multicell
  pair_with: sys_experience_mixed_pf_wrong_accounting
  pair_varies: [pf_accounting]
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 1
    ue_speed_kmh: 3.0
  system:
    duration_s: 5.0
    traffic_model: mixed
    file_bytes: 500000
    arrival_rate_hz: 5.0
    small_ue_share: 0.5
    small_file_bytes: 1500
    small_arrival_rate_hz: 20.0
    small_pdb_ms: 20.0
    large_pdb_ms: 300.0
    scheduler: pf
    pf_window_tti: 100
    pf_accounting: scheduled_tbs
    mu_enabled: false
    small_burst_policy: fractional_slot
    tdd_pattern: DDDSU
    neighbor_prb_util: 0.3
    neighbor_load_jitter: 0.05
    csi_aging: true
    srs_period_ms: 10.0
    srs_hopping: true
    csi_processing_delay_ms: 2.0
    olla_speedup: 1.0
    precoder: svd
    seed: 20260809
    num_replications: 16
  expect:
    measured: false
    invalidated_by: >-
      2026-09-03 合并容量/体验模式：legacy_v1 全带路径删除，所有场景改走
      experience_v2（容量口径 = traffic_model: full_buffer）。下面的数字是
      legacy_v1 测的，口径已变，**不再成立**。重跑 sr_probe_scenario 后再填。
    model_version: experience_v2
    dataset: ds_329edafb
    result: artifacts/results/experience_pf_accounting_crn_n16.json
    n_replications: 16
    large_burst_drb_throughput_mbps: [169.7562, 8.55]
    resource_utilization: [0.7741, 9.46]
    accounting_error_pct: 0.0
    note: >-
      2026-08-09 原理修复后正式 n=16 CRN。主指标 small FIFO 排队 P95：本臂
      0.5625 ms，故障注入臂 1.2109 ms，配对差 -0.6484 ms，95% CI
      [-0.9616,-0.3352]，Wilcoxon p=0.00322，门3通过。完成时延差
      -1.4937 ms、PDB miss 差 -0.0072 也通过。预注册外的 secondary 大 burst
      吞吐名义差 +2.7104 Mbps、CI [0.5181,4.9027]，未做多重性校正，不升级成
      主结论；资源利用率、总承载和积压不显著。两臂 32 次运行字节对账均为 0。

sys_experience_mixed_pf_wrong_accounting:
  group: 系统级 · 体验模式 PF 记账
  label: 大小文件混跑 · legacy full-band PF（故障注入）
  summary: >-
    故意把部分 RBG 用户按全带 best_se 更新 PF 平均速率的反向控制。它不是可部署方案，
    只用于证明 scheduled-TBS 记账不变量确实被调度器使用。
  answers:
    - 错误的全带 PF 记账是否会拉长小文件用户等待
    - 分配器是否真的在按需分 RBG
  channel_preset: company_64t4r_multicell
  pair_with: sys_experience_mixed_pf
  pair_varies: [pf_accounting]
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 1
    ue_speed_kmh: 3.0
  system:
    duration_s: 5.0
    traffic_model: mixed
    file_bytes: 500000
    arrival_rate_hz: 5.0
    small_ue_share: 0.5
    small_file_bytes: 1500
    small_arrival_rate_hz: 20.0
    small_pdb_ms: 20.0
    large_pdb_ms: 300.0
    scheduler: pf
    pf_window_tti: 100
    pf_accounting: legacy_fullband
    mu_enabled: false
    small_burst_policy: fractional_slot
    tdd_pattern: DDDSU
    neighbor_prb_util: 0.3
    neighbor_load_jitter: 0.05
    csi_aging: true
    srs_period_ms: 10.0
    srs_hopping: true
    csi_processing_delay_ms: 2.0
    olla_speedup: 1.0
    precoder: svd
    seed: 20260809
    num_replications: 16
  expect:
    measured: false
    invalidated_by: >-
      2026-09-03 合并容量/体验模式：legacy_v1 全带路径删除，所有场景改走
      experience_v2（容量口径 = traffic_model: full_buffer）。下面的数字是
      legacy_v1 测的，口径已变，**不再成立**。重跑 sr_probe_scenario 后再填。
    model_version: experience_v2_fault_injection
    dataset: ds_329edafb
    result: artifacts/results/experience_pf_accounting_crn_n16.json
    n_replications: 16
    large_burst_drb_throughput_mbps: [167.0458, 8.09]
    resource_utilization: [0.7738, 9.44]
    accounting_error_pct: 0.0
    note: >-
      2026-08-09 正式 n=16 CRN 的反向控制。它故意给部分 RBG 用户按全带记 PF，
      只用于验证不变量，**不得作为推荐配置**。与正确臂的主指标配对差及判决见
      sys_experience_mixed_pf 和 result JSON。

# ---------------------------------------------------------------------------
# 九、单小区基线（合并后唯一路径的实测锚点）
#
# 为什么新增：合并容量/体验模式后，原有 9 个多小区场景的锚点全部失效，而它们
# **当前跑不通**——`sr_system_sim` 要求数据集内所有 UE 同属一个 serving cell，
# 而 21 小区里撒 10 个 UE 每小区只落 1 个（要让单个小区拿到 10 个 UE，撒点量
# 需放大约 21 倍）。这三个单小区场景是能实跑、能重复、能当漂移哨兵的那部分。
# **它们不替代那 9 个多小区场景**：这里没有几何邻区干扰，邻区负载只有解析项。
# ---------------------------------------------------------------------------
sys_single_cell_capacity:
  group: 系统级 · 单小区基线
  label: 单小区容量口径 · 话务开到最大（full buffer）
  summary: >-
    合并后唯一路径的容量锚点。单站单扇区，无几何邻区干扰（邻区负载只进解析项），
    所以吞吐显著高于多小区场景，**不能和 sys_capacity_full_buffer 直接比**。
    体验类标准 KPI 按 TS 28.552 定义无样本（buffer 永不排空），报 None；
    容量口径看 cell_served_mbps 与 ITU 口径的 ue_served_p5_mbps。
  answers:
    - 合并后的单路径在满缓冲下吞吐/MCS/BLER 是多少
    - 标准 KPI 与工程 KPI 在满缓冲下各自给什么
  channel_preset: single_cell_64t4r
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 3.0
    # **这四类覆盖缺一不可**，否则 sr_system_sim 会分别在载波格栅、CSI 来源上硬失败：
    num_rb: 272                 # 预设默认 273，系统口径固定 272 = 17 RBG x 16 RB
    link: BOTH                  # SRS 是上行；只给 DL 拿不到 ul_srs_estimate
    srs_periodicity: 20
    srs_c_srs: 63
    srs_b_srs: 1
    srs_b_hop: 0
    channel_est_mode: ls_mmse
    lmmse_prior_mode: configured
  system:
    duration_s: 3.0
    traffic_model: full_buffer
    neighbor_prb_util: 0.3
    csi_aging: true
  expect:
    measured: true
    model_version: experience_v2
    dataset: ds_e94f1f09
    n_replications: 8
    cell_served_mbps: [618.6804, 0.1]
    avg_mcs_first_tx: [21.6953, 0.1]
    avg_rank: [2.0, 0.0]
    bler_first_tx: [0.0884, 1.4]
    serving_cell_prb_utilization: [1.0, 0.0]
    ue_served_p5_mbps: [19.5095, 0.4]
    note: >-
      2026-09-04 实测，8 次重复，dataset ds_e94f1f09（单小区 272 RB + SRS 来源）。
      格式 [均值, 95% 区间半宽占均值的百分比]。**这是三个新场景里统计最干净的一个**
      （所有半宽 <= 1.4%），因为 full buffer 没有话务随机性，唯一随机源是 HARQ 抽样。
      首传 BLER 0.0884 落在 10% 目标附近，说明 OLLA 在满缓冲下收敛良好。
      自洽核对：active_window_goodput_mbps 实测 61.97 ≈ cell_served/10 = 61.87，
      与「满缓冲下 UE 一直活跃、在传速率收敛到全窗平均」一致。
      **标准 KPI（drb_throughput_rel19_mbps / cell_experienced_mbps）为 None
      是设计意图**：TS 128 552 V19.5.0 p54 的样本只在 buffer emptied 事件上形成。

sys_single_cell_experience_ftp3:
  group: 系统级 · 单小区基线
  label: 单小区体验口径 · FTP3 轻载
  summary: >-
    合并后唯一路径的体验锚点。占用率约 0.28，标准与工程两套 KPI 同时有值，
    可用来核对「busy-period 吞吐」与「全窗平均」在轻载下相差一个数量级这件事。
    **当前 OLLA 未收敛（首传 BLER 约 2 倍目标），不得用本场景下 BLER 结论。**
  answers:
    - 轻载下标准 KPI 与工程 KPI 差多少
    - 合并后的单路径在有限话务下资源占用是多少
  channel_preset: single_cell_64t4r
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 3.0
    num_rb: 272
    link: BOTH
    srs_periodicity: 20
    srs_c_srs: 63
    srs_b_srs: 1
    srs_b_hop: 0
    channel_est_mode: ls_mmse
    lmmse_prior_mode: configured
  system:
    duration_s: 5.0
    traffic_model: ftp3
    arrival_rate_hz: 3.0
    neighbor_prb_util: 0.3
    csi_aging: true
  expect:
    measured: true
    model_version: experience_v2
    dataset: ds_e94f1f09
    n_replications: 8
    cell_served_mbps: [121.6753, 5.3]
    avg_mcs_first_tx: [16.009, 5.0]
    serving_cell_prb_utilization: [0.2906, 4.2]
    drb_throughput_rel19_mbps: [183.5624, 3.4]
    cell_experienced_mbps: [176.0849, 0.7]
    ue_experienced_p5_mbps: [49.7779, 3.0]
    ue_served_p5_mbps: [7.6679, 12.4]
    note: >-
      2026-09-04 实测，8 次重复，dataset ds_e94f1f09。
      **只记半宽站得住的项。** 未记的两个：bler_first_tx 实测 0.2066（半宽 15.7%，
      且是 10% 目标的 2 倍——低占用下 ACK/NACK 太少、OLLA 未收敛，olla_db_mean
      只压到 -3.24）；active_window_goodput_mbps 半宽 82.5%（在飞样本只有 0.9%，
      样本太少）。**这个场景当前不能用来下 BLER 结论**；要看稳态 BLER 请加长
      duration_s 或临时开 olla_speedup。
      口径对照：标准 busy-period 吞吐 183.6 Mbps 与 ITU 全窗口径 ue_served
      中位 12.2 Mbps 相差约 15 倍——前者是「burst 在传时多快」，后者是「UE 全时段
      平均拿到多少」，轻载下必然分开，**不可互相顶替**。

sys_single_cell_experience_mixed:
  group: 系统级 · 单小区基线
  label: 单小区体验口径 · 大小包混跑（mixed）
  summary: >-
    大小 UE 同场竞争的单小区锚点。占用率约 0.18，用于守住 mixed 话务在合并后
    仍能跑通并给出分层 KPI。**统计质量是三个里最差的**，多项半宽超过 30%，
    只作漂移哨兵，不得作为性能结论来源。
  answers:
    - mixed 话务在合并后的单路径上还跑不跑得通
    - 大小包混跑时分层 KPI 的量级
  channel_preset: single_cell_64t4r
  generate:
    num_ues: 10
    num_samples: 80
    num_slots_per_sample: 8
    ue_speed_kmh: 3.0
    num_rb: 272
    link: BOTH
    srs_periodicity: 20
    srs_c_srs: 63
    srs_b_srs: 1
    srs_b_hop: 0
    channel_est_mode: ls_mmse
    lmmse_prior_mode: configured
  system:
    duration_s: 5.0
    traffic_model: mixed
    neighbor_prb_util: 0.3
    csi_aging: true
  expect:
    measured: true
    model_version: experience_v2
    dataset: ds_e94f1f09
    n_replications: 8
    drb_throughput_rel19_mbps: [72.1381, 5.8]
    avg_mcs_first_tx: [18.9448, 6.7]
    ue_experienced_p5_mbps: [30.6176, 9.5]
    cell_experienced_mbps: [113.788, 10.2]
    note: >-
      2026-09-04 实测，8 次重复，dataset ds_e94f1f09。
      **大量指标因半宽过大被有意不记**：active_window_goodput_mbps 半宽 325.6%
      （在飞样本占比仅 0.16%，等于一两个样本）、drb_throughput_inflight_share
      123.2%、ue_served_median_mbps 34.7%、serving_cell_prb_utilization 23.6%、
      cell_served_mbps 14.4%。首传 BLER 实测 0.3677，是 10% 目标的 3.7 倍——
      占用率只有 0.175，OLLA 拿不到足够 ACK/NACK（olla_db_mean 才 -4.07）。
      **本场景当前只能当「还跑得通」的哨兵，不能下任何性能或 BLER 结论。**
      要让它可用：加长 duration_s 到 20 s 以上，或提高到达率把占用率抬到 0.3 以上。
