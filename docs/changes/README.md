# 改动文档

每条进入 `develop` 的实质改动一份，文件名 `YYYYMMDD-<模块>-<一句话>.md`。
**日常迭代不用写**；只在维护者说"同步 GitHub"时补齐本批次。

用途：公司 Agent 拿到 zip 快照后，靠这个目录快速理解"最近改了什么、为什么"，
而不是去通读 1200 行 CLAUDE.md 和 26 个测试文件。

## 模板（复制 `_TEMPLATE.md`）

五节固定，缺一不可。用无线语言写，不要贴代码。
